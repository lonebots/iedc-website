<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>IEDC Gec Thrissur</title>
    <%- include('../partials/teams/teamsLinks') %>
  </head>

  <body>
    <%- include('../partials/navbar/navbar') %>
    <h1>Teams Page</h1>
    <% team.data.forEach(function(obj) { %>
      <li>
        <strong><%= obj.name %></strong>
        post <%= obj.post %>, teamType <%= obj.teamType %>,Image <%= obj.image %>
      </li>
      <% }); %>
    <%- include('../partials/teams/teamsScripts') %>
  </body>
</html>